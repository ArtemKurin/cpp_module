Materia:
	clone()

	use(ICharacter&):
		- Ice: "* стреляет ледяным болтом в <имя> *".
		- Cure: "* исцеляет раны <имя> *".

Character:
	Персонаж имеет инвентарь из 4 слотов, что означает максимум 4 материи.
	При создании инвентарь пуст. Персонаж экипирует материями первый пустой слот.
	unequip() 
	use(int, ICharacter&)
	 	по адресу slot[idx], и передать целевой параметр в функцию AMateria::use

MateriaSource:
	- learnMateria(AMateria*)
		Копирует Материю, переданную в качестве параметра, и сохраняет ее в памяти, чтобы ее можно было клонировать(не более 4шт)
	- createMateria(std::string const &)
		Возвращает новую Материю. Последняя является копией материи, ранее изученной MateriaSource, чей тип равен типу, переданному в качестве параметра. Возвращает 0, если тип неизвестен.
